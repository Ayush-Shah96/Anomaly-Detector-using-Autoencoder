# Anomaly-Detector-using-Autoencoder
This project implements an anomaly event detection system using an Autoencoder neural network. The model learns the normal patterns in data and identifies anomalies based on reconstruction error. Events with high reconstruction error are flagged as anomalous.
